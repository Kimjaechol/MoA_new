# ZeroClaw Railway Environment Variables
#
# Copy this file into your Railway service environment settings.
# All values below are examples — replace with your actual credentials.
#
# SECURITY NOTE: Never commit real API keys or secrets to version control.
# Use Railway's encrypted environment variable UI or `railway variables set`.

# ── LLM Provider Configuration ────────────────────────────────
# API key for your LLM provider (OpenRouter, Anthropic, OpenAI, etc.).
ZEROCLAW_API_KEY=your_openrouter_or_anthropic_key

# Default provider backend. Options: openrouter, anthropic, openai, ollama
ZEROCLAW_DEFAULT_PROVIDER=openrouter

# Default model to use for inference.
# OpenRouter format: <provider>/<model-name>
ZEROCLAW_DEFAULT_MODEL=anthropic/claude-sonnet-4

# ── Gateway / Network ─────────────────────────────────────────
# Bind address: 0.0.0.0 is required for Railway to route traffic to the container.
ZEROCLAW_HOST=0.0.0.0

# Gateway port. Railway injects its own PORT variable; this should match.
ZEROCLAW_PORT=3000

# Allow binding to all interfaces (required inside containers).
ZEROCLAW_ALLOW_PUBLIC_BIND=true

# ── Security ──────────────────────────────────────────────────
# Require device pairing before accepting agent commands.
ZEROCLAW_REQUIRE_PAIRING=true

# ── Cloudflare R2 (Optional — for binary distribution) ────────
# Only needed if you use R2 for hosting release binaries.
R2_BUCKET_URL=https://your-bucket.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key

# ── Channel Tokens (Optional — enable as needed) ─────────────
# Set the primary token env var to activate each channel.
# ALLOWED_USERS: comma-separated user IDs, or "*" to allow all.

# Telegram (from @BotFather)
# ZEROCLAW_TELEGRAM_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# ZEROCLAW_TELEGRAM_ALLOWED_USERS=*

# Discord (from Discord Developer Portal)
# ZEROCLAW_DISCORD_TOKEN=your_discord_bot_token
# ZEROCLAW_DISCORD_ALLOWED_USERS=*

# Slack (from Slack App settings)
# ZEROCLAW_SLACK_TOKEN=xoxb-your-slack-bot-token
# ZEROCLAW_SLACK_APP_TOKEN=xapp-your-slack-app-token
# ZEROCLAW_SLACK_ALLOWED_USERS=*

# WhatsApp (from Meta Business Suite)
# ZEROCLAW_WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token
# ZEROCLAW_WHATSAPP_PHONE_NUMBER_ID=123456789
# ZEROCLAW_WHATSAPP_VERIFY_TOKEN=your_verify_token
# ZEROCLAW_WHATSAPP_ALLOWED_NUMBERS=*

# LINE (from LINE Developers Console)
# ZEROCLAW_LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token
# ZEROCLAW_LINE_CHANNEL_SECRET=your_line_channel_secret
# ZEROCLAW_LINE_ALLOWED_USERS=*

# KakaoTalk (from Kakao Developers Console)
# ZEROCLAW_KAKAO_REST_API_KEY=your_kakao_rest_api_key
# ZEROCLAW_KAKAO_ADMIN_KEY=your_kakao_admin_key
# ZEROCLAW_KAKAO_ALLOWED_USERS=*

# Lark / Feishu (from Lark Developer Console)
# ZEROCLAW_LARK_APP_ID=cli_your_app_id
# ZEROCLAW_LARK_APP_SECRET=your_lark_app_secret
# ZEROCLAW_LARK_ALLOWED_USERS=*

# DingTalk (from DingTalk Developer Console)
# ZEROCLAW_DINGTALK_CLIENT_ID=your_dingtalk_client_id
# ZEROCLAW_DINGTALK_CLIENT_SECRET=your_dingtalk_client_secret
# ZEROCLAW_DINGTALK_ALLOWED_USERS=*

# QQ (from QQ Bot Platform)
# ZEROCLAW_QQ_APP_ID=your_qq_app_id
# ZEROCLAW_QQ_APP_SECRET=your_qq_app_secret
# ZEROCLAW_QQ_ALLOWED_USERS=*

# Matrix
# ZEROCLAW_MATRIX_ACCESS_TOKEN=your_matrix_access_token
# ZEROCLAW_MATRIX_HOMESERVER=https://matrix.org
# ZEROCLAW_MATRIX_ROOM_ID=!room_id:matrix.org
# ZEROCLAW_MATRIX_ALLOWED_USERS=@user:matrix.org

# Signal (via signal-cli REST API)
# ZEROCLAW_SIGNAL_HTTP_URL=http://localhost:8080
# ZEROCLAW_SIGNAL_ACCOUNT=+1234567890
# ZEROCLAW_SIGNAL_ALLOWED_FROM=+1234567890

# ── Observability (Optional) ──────────────────────────────────
# OpenTelemetry OTLP endpoint for trace and metric export.
# OTEL_EXPORTER_OTLP_ENDPOINT=https://your-otel-collector:4317

# Log level filter (trace, debug, info, warn, error).
# RUST_LOG=zeroclaw=info
